# 14云负载均衡

---

## 📋 内容汇总
本文档全面介绍了云负载均衡（CLB）的产品概念、技术原理、架构演进以及实际操作。涵盖从单节点到LVS+Nginx的架构演进、四层/七层负载均衡的技术实现、负载均衡算法（轮询、最小连接数、源地址散列）、健康检查、会话保持等核心功能。

## 🎯 知识要点
- 负载均衡定义：将负载分摊到多个操作单元，扩展带宽、提高可用性
- 架构演进：单节点→主备高可用→LVS→LVS+Nginx的演进过程
- 技术原理：四层基于TGW（类似LVS），七层基于STGW（类似Nginx）
- CLB分类：内网负载均衡、公网IPv4/IPv6负载均衡、NAT64负载均衡
- 核心组件：负载均衡实例、监听器、后端服务器三部分组成
- 均衡算法：加权轮询、加权最小连接数、源地址散列三种算法
- 健康检查：定期探测后端服务器状态（ping/tcp/http），确保服务可用性
- 会话保持：同一IP请求转发到同一后端服务器，支持购物车等应用场景

## 💡 重点难点
- 四层与七层区别：四层基于IP+端口转发，七层基于HTTP/HTTPS协议内容转发
- TGW与STGW关系：TGW类似LVS处理四层流量，STGW类似Nginx处理七层流量
- 均衡算法选择：根据业务场景选择轮询（均匀分布）、最小连接数（动态负载）、源地址散列（会话保持）
- NAT64技术：IPv6客户端访问IPv4后端服务器的无缝升级方案
- 跨域绑定：不同地域负载均衡与后端服务器的绑定限制和计费规则

---

## 第 1 页

云负载均衡CLB
产品培训


## 第 2 页

目录
2、应用场景 & 计费指南
3、操作指南 & 最佳实践
1、产品介绍 & 原理架构
4、常见问题


## 第 3 页

1
产品介绍 & 原理架构
PART ONE


## 第 4 页

什么是负载均衡
负载均衡，英文名称为Load Balance，其含义就是指将负载（工作任务）进行平衡、分摊到多个操作单元上进行运行，例如FTP服务器、Web服务器、企业核心应用服务器和其它主要任务服务器等，从而协同完成工作任务。
负载均衡构建在原有网络结构之上，它提供了一种透明且廉价有效的方法扩展服务器和网络设备的带宽、加强网络数据处理能力、增加吞吐量、提高网络的可用性和灵活性。
应用服务器1
192.168.1.10
应用服务器2
192.168.1.20
负载均衡
公网IP：22.2.2.4
NAT
内网IP：192.168.1.254
客户端PC
应用服务器n
192.168.1.n
客户端手机
客户端IPAD


## 第 5 页

负载均衡的由来
单节点业务架构
只有1台服务器提供服务，若服务器故障，业务中断
无法承受更高并发
纵向升级需停机


## 第 6 页

负载均衡的由来
高可用业务架构
优点：
主备2台服务器，若主服务器故障，可切换到 备 继续提供服务，保证业务不中断
缺点：
浪费资源
无法承受更高并发
不支持横向扩容，纵向扩容需停机


## 第 7 页

负载均衡的由来
LVS负载均衡业务架构
优点：
多台服务器同时提供服务，保证业务不中断
不停机横向扩容，保证业务不停机的前提下，提供更高的业务并发
缺点：
浪费资源
不支持7层协议（http/https）转发，若后端服务器业务不一样，无法实现自定义转发，即将某个用户的请求转发给后端指定的业务服务器


## 第 8 页

负载均衡的由来
LVS + Nginx负载均衡业务架构
优点：
多台服务器同时提供服务，保证业务不中断
不停机横向扩容，保证业务不停机的前提下，提供更高的业务并发
同时支持4层、7层协议转发
缺点：
浪费资源


## 第 9 页

什么是云负载均衡CLB
负载均衡（Cloud Load Balancer，CLB）提供安全快捷的流量分发服务，访问流量经由 CLB 可以自动分配到云中的多台后端服务器上，扩展系统的服务能力并消除单点故障。负载均衡支持亿级连接和千万级并发，可轻松应对大流量访问，满足业务需求。
云应用服务器1
192.168.1.10
云应用服务器2
192.168.1.20
云负载均衡CLB
公网IP：22.2.2.4
内网IP：192.168.1.254
客户端PC
云应用服务器n
192.168.1.n
客户端手机
客户端IPAD


## 第 10 页

CLB的技术原理
云负载均衡当前提供四层和七层的负载均衡服务，通过配置监听器来实现业务流量转发优点：
四层主要基于自研的统一接入网关（Tencent Gateway，TGW）来实现负载均衡。TGW 具有可靠性高、扩展性强、性能高、抗攻击能力强等特点，支持 Data Plane Development Kit（DPDK）高性能转发，单集群可支持亿级并发、千万级 PPS。内部诸多业务均通过 TGW 接入服务，包括腾讯游戏、腾讯视频、微信、QQ 等。
七层主要基于 Secure Tencent Gateway（STGW）实现负载均衡，STGW 是腾讯基于 Nginx 自研的支持大规模并发的七层负载均衡服务，承载了腾讯内大量的七层业务流量，包括腾讯新闻、理财通、腾讯游戏、微信等。
总结：TGW 类似 LVS，STGW类似Nginx，云负载均衡CLB可以简单理解是   ‘云服务器部署 TGW+STGW 后封装的产品’ 。


## 第 11 页

CLB的优势
高性能
CLB 单集群（非单个 CLB 实例）支持亿级并发连接数，每秒处理百万级的包数据量，轻松应对日访问量超过千万的电商网站、社交平台和游戏业务等。
高可用
CLB 采用集群化部署，可用性高达99.95%。在单台 CLB 物理服务器可用的极端情况下，仍可支撑千万级的并发连接数。同时，集群系统会及时剔除故障实例，筛选出健康实例，确保后端服务器业务正常运行。
高弹性
CLB 集群根据业务负载横向伸缩，弹性扩展应用系统对外的服务能力，同时借助弹性伸缩（Auto Scaling）的动态伸缩组自动创建和释放 CVM 实例，结合动态监控情况和秒级计费系统，您无需手工干预和预估资源，即可实现您的计算资源合理分配，防止资源浪费。
安全稳定
CLB 依靠大禹分布式防御系统能够防御绝大多数网络攻击（例如 DDoS、Web 入侵），针对流量攻击实现秒级清洗，极大避免 IP 被封、带宽被占满等情况发生。CLB 自带的 synproxy 防攻击机制，避免了大禹系统生效之前后端 CVM 被攻击压垮，保护数据更安全稳定。
CLB 对每个租户的流量进行严格隔离，提供主动 DDoS 防护能力，公网 CLB 默认支持 DDoS 基础防护。CLB 还支持 DDoS 高防包、DDoS 高防 IP、Web 应用防火墙 等多种安全产品，保障业务安全性。
低成本
使用云 CLB，您无需再投入额外的负载均衡硬件，无需投入过多繁琐的运维工作，为您节省高达99%的硬件费用和人力支出。CLB 支持多种计费模式，可按需选择计费模式。
01
02
03
04
05


## 第 12 页

CLB的分类
内网负载均衡
整个集群访问环境全部都是内网IPV4地址通信，但后续可绑定弹性公网IPV4/IPV6，实现公网访问
公网IPV4负载均衡
整个集群访问环境全部都是公网IPV4地址通信
公网IPV6负载均衡
整个集群访问环境全部都是公网IPV6地址通信
公网IPV6 NAT64 负载均衡
客户端是公网IPV6地址，后面服务器是内网IPV4地址， 负载均衡既有公网IPV6地址提供公网访问，也有内网IPV4地址提供内网访问，可将 公网IPV4业务集群 无缝升级 公网IPV6业务集群
云应用服务器1
192.168.1.10
云应用服务器2
192.168.1.20
云负载均衡CLB
公网IPV4：22.2.2.4
NAT
内网IP：192.168.1.254
IPV4客户端PC/手机
云应用服务器n
192.168.1.n
CLB IPV6 NAT64
公网IPV6：fe80::fe80
NAT64
内网IP：192.168.1.253
IPV6客户端PC/手机


## 第 13 页

CLB的组成
一个提供服务的负载均衡组通常由以下部分组成：
负载均衡实例：一个 CLB 实例是一个运行的负载均衡服务，用于流量分发。
监听器：用来检查客户端请求并将请求转发给后端服务器。
后端服务器：后端的一组服务器实例，用于接收前端的请求。
来自负载均衡外的访问请求，通过负载均衡实例并根据相关的策略和转发规则分发到后端服务器进行处理。
（下图以腾讯云为例）


## 第 14 页

2
应用场景 & 计费指南
PART TWO


## 第 15 页

应用场景
流量分发和消除单点故障


## 第 16 页

应用场景
横向扩展
结合弹性伸缩可实现横线自动扩缩容


## 第 17 页

应用场景
结合云解析DNS可实现全局负载均衡


## 第 18 页

计费指南
负载均衡 CLB 的费用涉及四部分：
CLB 实例费、网络费、性能容量单位 LCU 费用、跨域绑定费。


## 第 19 页

3
操作指南 & 最佳实践
PART THREE


## 第 20 页

标准账号CLB创建


## 第 21 页

操作指南
CLB操作以监听器配置为核心：
4层监听器转发
7层监听器创建，自定义规则转发
具体配置细节请参考官网文档
或根据控制台提示自行配置


## 第 22 页

操作指南
1、均衡方式
均衡方式是负载均衡向后端服务器分配流量的算法，根据不同的均衡方式可以达到不同的均衡效果。CLB有以下三种分配流量的算法：
加权轮询算法：以轮叫的方式，依次请求调度不同的服务器。根据新建连接数来调度，权值高的服务器先收到连接，权重值越高被轮询到的次数（概率）也越高，相同权值的服务器处理相同数目的连接数。
加权最小连接数算法：是一种动态调度算法，与轮询调度算法相反，它通过服务器当前所活跃的连接数来估计服务器的负载情况。在最小连接数调度算法的基础上，根据服务器的不同处理能力，给每个服务器分配不同的权值，使其能够接受相应权值数的服务请求，是在最小连接数调度算法的基础上的改进。
源地址散列调度算法：根据请求的源 IP 地址，使用散列键（Hash Key）从静态分配的散列表找出对应的服务器，若该服务器为可用且未超载状态，则请求发送到该服务器，反之则返回空。
监听器核心功能选择
2、健康检查
负载均衡可以定期像后端服务器发送探测请求（ping、tcp、http）、尝试连接或发送请求来探测后端服务器运行的状况，这些探测称为健康检查。负载均衡通过健康检查来判断后端服务的可用性，避免后端服务异常影响前端业务，从而提高业务整体可用性。
3、会话保持
会话保持可使得来自同一 IP 的请求被转发到同一台后端服务器上。默认情况下，负载均衡会将每个请求分别路由到不同后端服务器实例负载。但是，可以使用会话保持功能使特定用户的请求被路由到同一台后端服务器实例上，这样可以使某些需要保持会话的应用程序（如购物车）合理地工作。


## 第 23 页

最佳实践
1、创建1个负载均衡
2、配置4层TCP协议监听器，并实现访问
3、配置7层http协议监听器，实现多个域名的转发，并分别实现访问
4、配置7层https协议监听器，配置多个SSL证书，实现多个域名的转发，并分别实现访问
5、配置http跳转https
6、配置个性化参数，实现负载均衡转发后端超时时间配置为120s


## 第 24 页

4
常见问题
PART FOUR


## 第 25 页

常见问题
1、四层负载均衡和七层负载均衡有什么区别？
2、负载均衡绑定的的后端云服务器需要公网带宽吗？是否会影响负载均衡的服务？
3、负载均衡与后端服务器之间的通讯是走的内网还是外网？
4、负载均衡 Cookies 会话保持方式的原理是什么？
5、权重置为0与解绑 RS 有什么区别？
6、健康检查提示云服务器异常该如何处理？
7、如何在 CLB 上开启 WS/WSS？


## 第 26 页

懂云 更懂服务
